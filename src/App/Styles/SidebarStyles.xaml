<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="SidebarAccentBlueColor">#0078D4</Color>
    <Color x:Key="SidebarSelectedBgColor">#E8F4FD</Color>
    <Color x:Key="SidebarHoverBgColor">#F3F4F6</Color>
    <Color x:Key="SidebarNavTextColor">#374151</Color>
    <Color x:Key="SidebarSubtitleColor">#6B7280</Color>

    <SolidColorBrush x:Key="SidebarAccentBlueBrush" Color="{StaticResource SidebarAccentBlueColor}"/>
    <SolidColorBrush x:Key="SidebarSelectedBgBrush" Color="{StaticResource SidebarSelectedBgColor}"/>
    <SolidColorBrush x:Key="SidebarHoverBgBrush" Color="{StaticResource SidebarHoverBgColor}"/>
    <SolidColorBrush x:Key="SidebarNavTextBrush" Color="{StaticResource SidebarNavTextColor}"/>
    <SolidColorBrush x:Key="SidebarSubtitleBrush" Color="{StaticResource SidebarSubtitleColor}"/>

    <Style x:Key="SidebarNavButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource SidebarNavTextBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="14,12"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="{StaticResource SidebarAccentBlueBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SidebarHoverBgBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SidebarSelectedBgBrush}"/>
                            <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                            <Setter Property="Foreground" Value="{StaticResource SidebarAccentBlueBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SidebarSelectedBgBrush}"/>
                            <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                            <Setter Property="Foreground" Value="{StaticResource SidebarAccentBlueBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Background" Value="#F3F4F6"/>
                            <Setter Property="Foreground" Value="#9CA3AF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Per-nav-key styles so selection stays visible when focus moves to content -->
    <Style x:Key="SidebarNavButtonConnectStyle" TargetType="Button" BasedOn="{StaticResource SidebarNavButtonStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CurrentNavKey}" Value="ConnectDevices">
                <Setter Property="Background" Value="{StaticResource SidebarSelectedBgBrush}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Foreground" Value="{StaticResource SidebarAccentBlueBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="SidebarNavButtonAudiogramStyle" TargetType="Button" BasedOn="{StaticResource SidebarNavButtonStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CurrentNavKey}" Value="Audiogram">
                <Setter Property="Background" Value="{StaticResource SidebarSelectedBgBrush}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Foreground" Value="{StaticResource SidebarAccentBlueBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="SidebarNavButtonFittingStyle" TargetType="Button" BasedOn="{StaticResource SidebarNavButtonStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CurrentNavKey}" Value="Fitting">
                <Setter Property="Background" Value="{StaticResource SidebarSelectedBgBrush}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Foreground" Value="{StaticResource SidebarAccentBlueBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="SidebarNavButtonSessionSummaryStyle" TargetType="Button" BasedOn="{StaticResource SidebarNavButtonStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CurrentNavKey}" Value="SessionSummary">
                <Setter Property="Background" Value="{StaticResource SidebarSelectedBgBrush}"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Foreground" Value="{StaticResource SidebarAccentBlueBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
