using System.Windows;

namespace Ul8ziz.FittingApp.App.Views
{
    /// <summary>Non-modal waiting window shown during session end when saving to device.</summary>
    public partial class SessionEndWaitingWindow : Window
    {
        public SessionEndWaitingWindow()
        {
            InitializeComponent();
        }
    }
}
